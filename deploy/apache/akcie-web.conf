<VirtualHost *:80>
    ServerName akcie.your-domain.com

    ProxyPreserveHost On
    ProxyPass / http://127.0.0.1:5000/
    ProxyPassReverse / http://127.0.0.1:5000/

    # Recommended headers
    RequestHeader set X-Forwarded-Proto "http"
    RequestHeader set X-Forwarded-Port "80"

    # Optional basic auth (uncomment + configure)
    # <Location />
    #     AuthType Basic
    #     AuthName "Restricted Access"
    #     AuthUserFile /path/to/.htpasswd
    #     Require valid-user
    # </Location>

    ErrorLog ${APACHE_LOG_DIR}/prague-stock-tracker_error.log
    CustomLog ${APACHE_LOG_DIR}/prague-stock-tracker_access.log combined
</VirtualHost>

# Optional HTTPS configuration
#<VirtualHost *:443>
#    ServerName akcie.your-domain.com
#    Protocols   h2 http/1.1 acme-tls/1
#
#    ErrorLog ${APACHE_LOG_DIR}/akcie-error.log
#    CustomLog ${APACHE_LOG_DIR}/akcie-access.log combined
#    
#    ProxyRequests Off
#    ProxyPreserveHost On
#  
#    SSLEngine On
#  
#    SSLCertificateFile /etc/pki/tls/letsencrypt/akcie.your-domain.com-cert.pem
#    SSLCertificateKeyFile /etc/pki/tls/letsencrypt/akcie.your-domain.com-key.pem
#    SSLCertificateChainFile /etc/pki/tls/letsencrypt/akcie.your-domain.com-fullchain.pem
#  
#    RewriteEngine On
#    RewriteCond %{HTTPS} off
#    RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI}
#  
#    <Proxy *>
#      Order deny,allow
#      Allow from all
#    </Proxy>
#
#    ProxyPreserveHost On
#    ProxyPass / http://127.0.0.1:5000/
#    ProxyPassReverse / http://127.0.0.1:5000/
#    
#    # Optional basic auth (uncomment + configure)
#    # <Location />
#    #     AuthType Basic
#    #     AuthName "Super secret"
#    #     AuthUserFile /etc/httpd/conf/htpasswd
#    #     Require valid-user
#    # </Location>
#</VirtualHost>